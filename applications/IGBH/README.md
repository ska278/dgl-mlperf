runs.sh -  master run script  
- it contains command lines for partition creation and distirbuted model training
- modify the arguments like input graph and paritions path accordingly

create_partition.py  
- creates static edge-cut partitions of the input hetero graph (homo graph partition code needs some fixes)
- Random graph partition code is WIP  
